services:
  insprira:
    # 生产部署：使用已构建好的镜像，不挂载源码
    image: insprira:latest
    container_name: insprira
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
    # 只挂载数据目录（SQLite、缓存），不挂载源码
    volumes:
      - ./data:/data
